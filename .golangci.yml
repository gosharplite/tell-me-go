version: "2"

linters:
  enable:
    - exhaustive
  settings:
    staticcheck:
      checks:
        - "SA"
        - "-SA5011"
    exhaustive:
      default-signifies-exhaustive: true
      explicit-exhaustive-switch: true
